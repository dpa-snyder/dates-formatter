// Phase 5 — full Converter screen implementation.
// For now renders a placeholder so the app shell compiles and runs.
export default function Converter() {
  return (
    <div className="screen">
      <div className="screen-title">Date Formatter</div>
      <div className="screen-subtitle">
        Standardize date columns in archival Excel and CSV spreadsheets.
      </div>
      <div className="placeholder">
        <h2>Converter — coming in Phase 5</h2>
        <p>Go engine (Phase 2) → Excel I/O (Phase 3) → App bindings (Phase 4) → UI (Phase 5)</p>
      </div>
    </div>
  )
}
